!#/bin/sh
set -e

if [ "$(id -u)" -ne "0" ]; then
  echo "Run me as sudo."

  exit 1
fi

ufw allow ssh

cat host.pub >> "/etc/sshd/authorized_keys"

# SSH
systemctl enable ssh
systemctl start ssh
